<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout_deaf"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_feelspeak_offwhite">

    <!-- MAIN AREA (toolbar + deaf content) -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBarDeaf"
            style="@style/Widget.MaterialComponents.Toolbar"
            android:layout_width="match_parent"
            android:layout_height="85dp"
            android:minHeight="?attr/actionBarSize"
            android:background="#682B2B"
            android:paddingTop="30dp"
            android:paddingBottom="10dp"
            app:navigationIcon="@drawable/ic_menu"
            app:navigationIconTint="@android:color/white">

            <!-- Center cursive title -->
            <TextView
                android:id="@+id/toolbarTitleDeaf"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal|bottom"
                android:layout_marginBottom="5dp"
                android:fontFamily="@font/feelspeak_cursive"
                android:text="FeelSpeak"
                android:textColor="@android:color/white"
                android:textSize="32sp" />
        </com.google.android.material.appbar.MaterialToolbar>

        <!-- Deaf screen content -->
        <LinearLayout
            android:id="@+id/content_deaf_root"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="85dp"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/bg_feelspeak_offwhite"
            android:padding="24dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Deaf Person Mode"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="#000000"
                android:gravity="center"
                android:layout_marginBottom="48dp"/>

            <Button
                android:id="@+id/btnRecordVideo"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="📹 Record Video"
                android:background="@drawable/btn_red_gradient"
                app:backgroundTint="@null"
                android:textSize="20sp"
                android:layout_marginBottom="24dp" />

            <Button
                android:id="@+id/btnUploadVideo"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="📁 Upload Video"
                android:background="@drawable/btn_orange_gradient"
                app:backgroundTint="@null"
                android:textSize="20sp"
                android:layout_marginBottom="24dp" />

        </LinearLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- LEFT DRAWER (reuse same drawer layout) -->
    <include
        android:id="@+id/left_drawer_container_deaf"
        layout="@layout/layout_right_drawer"
        android:layout_width="350dp"
        android:layout_height="match_parent"
        android:layout_gravity="start" />

</androidx.drawerlayout.widget.DrawerLayout>